package DAO;

import Entities.Books.Books;
import Entities.Human.Person;

import java.io.IOException;

public class MainDAO extends AbstractSave{
    @Override
    public void Save(Object o) throws IOException {
        super.Save(o);
    }

}
